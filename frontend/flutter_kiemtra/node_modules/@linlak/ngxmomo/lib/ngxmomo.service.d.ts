import { NgxMomoServiceConfig, MomoEventMain } from './data';
import { HttpClient } from '@angular/common/http';
import { Observable, BehaviorSubject } from 'rxjs';
import { MomoProvider } from './entities/momo-provider';
export declare class NgxMomoService {
    private http;
    private providers;
    eventListener: BehaviorSubject<any>;
    listen(): Observable<MomoEventMain | any>;
    private eventCallback;
    constructor(config: NgxMomoServiceConfig, http: HttpClient);
    getProvider(tagName: string): MomoProvider;
    private notifyEvt;
}
