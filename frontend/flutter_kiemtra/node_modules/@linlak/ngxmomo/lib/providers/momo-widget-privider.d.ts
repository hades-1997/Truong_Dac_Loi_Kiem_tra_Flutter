import { BaseMomoProvider } from '../entities/base-momo-provider';
import { MomoProviderClass, MtnMomoConfig } from '../entities/momo-provider';
export declare class MomoWidgetProvider extends BaseMomoProvider {
    static readonly PROVIDER_TYPE = "mtnmomowidget";
    TYPE: string;
    private momoConfig;
    momoProviderObj: MomoProviderClass;
    constructor(config: MtnMomoConfig);
    initialize(eventCallback: any): Promise<any>;
    getApiUser(): string;
    getCurrency(): string;
}
